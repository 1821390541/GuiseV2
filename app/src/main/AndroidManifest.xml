<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 位置权限 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- 百度地图需要的权限 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="GuiseV2"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuiseV2"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">

        <!-- 高德地图SDK -->
        <!-- ⚠️ 重要提示：当前Key e3ad31d868b06dff03b01c2cc8f3274d 是调试用Key，
             需要使用与您当前应用签名匹配的Key才能正常显示地图底图。
             如果地图页显示空白，请在 https://lbs.amap.com/ 注册并替换为您自己的Key。
             注册时需提供应用包名（com.houvven.guisev2）和当前签名的SHA1值。 -->
        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="e3ad31d868b06dff03b01c2cc8f3274d" />

        <!-- Xposed模块声明 -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="虚拟定位模块 - 支持百度地图、自定义经纬度、设备信息伪装" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />

        <!-- 主Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.GuiseV2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>